# ⚙️ Cpp Craft Code Generator **Cpp Craft** — это интерактивный конструктор кода на C++ с удобным меню. Позволяет быстро собирать структуру программы: от подключения библиотек до создания классов, не написав ни строчки кода вручную. --- ## 🚀 Возможности проекта Проект генерирует код для следующих элементов C++: | Команда | Элемент | Описание | |---------|---------|----------| | `1` | **Библиотеки** | Выбор из 89 STL заголовков (по номеру или имени). Автоматически добавляет `<iostream>` и `<string>`. | | `2` | **Переменные** | Типы: `int`, `double`, `float`, `string`, `bool`, `auto`. | | `3` | **Циклы** | `for`, `while`, `do-while` с шаблонами. | | `4` | **switch-case** | Конструкция `switch` с указанным числом `case`. | | `5` | **if** | Блоки `if`. | | `6` | **else if** | Цепочки `else if`. | | `7` | **Массивы `arr[]`** | Одномерные статические массивы. | | `8` | **Массивы `arr[][]`** | Двумерные массивы. | | `9` | **Динамические массивы** | Выделение памяти через `new` + автоматический `delete[]`. | | `10` | **Функции** | Любой возвращаемый тип (`void`, `int`, `double`, `string`, `bool`), параметры, тело функции. | | `11` | **vector** | `std::vector` с типом и размером. | | `12` | **fstream** | `fstream`, `ifstream`, `ofstream` для работы с файлами. | | `13` | **ООП** | Создание `class` и `struct` с секциями `public`/`private`. | ### 🔧 Дополнительные команды: - `0` — сохранить файл и открыть его (Windows: `start`) - `-1` — сохранить файл без открытия - `-2` — предпросмотр кода в консоли - `-3` — выйти без сохранения (файл удаляется) --- ## 🖥️ Системные требования - **ОС:** Linux / macOS / Windows ⚠️ В коде используется `system("clear")` (для Linux/macOS). На Windows нужно заменить на `system("cls")`. - **Компилятор:** C++11 и выше (`g++`, `clang++`, `MSVC`) - **Стандартная библиотека C++** (STL) --- ## 📁 Структура проекта ``` CppCraft/ │ ├── main.cpp # Исходный код генератора ├── README.md # Этот файл └── (сгенерированные файлы) # Например, my_program.cpp ``` ### Логика работы: 1. Пользователь вводит имя будущего `.cpp` файла. 2. В цикле отображается меню с выбором элементов. 3. Каждый элемент добавляется в соответствующий вектор (`vector_library`, `vector_variable` и т.д.). 4. При сохранении все векторы записываются в файл в правильном порядке: - Библиотеки - Функции - Классы и структуры - `main()` с: * переменными * циклами * `switch-case` * `if` / `else if` * массивами * `vector` * `fstream` --- ## 📜 Лицензия **© 2025 N.Y.RPO3 (проект Cpp Craft). Все права защищены.** Данное программное обеспечение и его исходный код являются интеллектуальной собственностью автора. Запрещается: - Копирование, распространение или модификация кода без явного письменного разрешения автора. - Использование кода в коммерческих целях. - Выдача данного кода за свой собственный. > 📧 По вопросам использования обращаться к автору проекта. --- ## ✍️ Пример использования ``` name file(don't write .cpp) :_> my_first_program   }--------------------------------{ }-----Cpp Craft Code By N.Y.RPO3-----{   }--------------------------------{ ____________________________________ | [1]-libraries   [10]-function    | | [2]-variables   [11]-vector      | | [3]-cycles      [12]-fstream     | | [4]-switch_case [13]-OOP         | | [5]-if                           | | [6]-else if   [-3]-exit no create| | [7]-arr[]     [-2]-check result  | | [8]-arr[][]   [-1]-save and exit | | [9]-d_arr     [ 0]-save and start| |-----------------------------------| | L------------->answer: 1 ``` После выбора элементов и ввода `0` будет создан файл `my_first_program.cpp` с полной структурой программы. --- ## 🧠 Идеи для доработки - Поддержка `system("cls")` для Windows (определение ОС через макросы). - Добавление шаблонов для часто используемых алгоритмов. - Генерация `CMakeLists.txt`. - Экспорт в ZIP-архив. --- **Cpp Craft — твой код за минуту!** ⚡ 